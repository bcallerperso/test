# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a blank starter project with only a `README.md`. There are no application services, dependencies, build steps, or tests to run.

When application code is added, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run the dev server
- How to run lint and tests
- Any non-obvious startup caveats
